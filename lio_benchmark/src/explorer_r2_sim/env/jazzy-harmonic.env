# ROS 2 Jazzy + Gazebo Harmonic. Default; well-tested combo.
# Use:  ln -sf env/jazzy-harmonic.env .env  (then docker compose build sim)
ROS_DISTRO=jazzy
GZ_PKG=gz-harmonic
GZ_CMD=gz sim
ROS_DOMAIN_ID=42
